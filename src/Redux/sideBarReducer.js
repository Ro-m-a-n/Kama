let initialState = {
  friendsNavData: [
    // { id: 1, name: "Roman" },
    // { id: 2, name: "Artem" },
    // { id: 3, name: "Vasia" },
  ],
};

let sideBarReducer = (state = initialState, action) => {
  return state;
};
export default sideBarReducer;
